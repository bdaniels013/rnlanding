services:
  - type: web
    name: rnlanding
    env: node
    plan: free
    buildCommand: npm install && npm run db:generate && npm run build:prod
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXTAUTH_URL
        value: https://rnlanding.onrender.com
      - key: APP_URL
        value: https://rnlanding.onrender.com
      - key: FRONTEND_URL
        value: https://rnlanding.onrender.com
    healthCheckPath: /

databases:
  - name: richnick-db
    plan: free
    env: postgresql
